<div class="w-layout-cell routeslist">
    <div class="route-list">
        @foreach($routesFilesList as $routeFile)
            <div class="route">{{ $routeFile }}</div>
        @endforeach
    </div>
</div>
